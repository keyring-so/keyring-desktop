name: Release
on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        build: [
          {name: keyring-wallet-linux, platform: linux/amd64, os: ubuntu-latest},
          {name: keyring-wallet-windows.exe, platform: windows/amd64, os: windows-latest},
          {name: keyring-desktop, platform: darwin/universal, os: macos-latest}
        ]
    runs-on: ${{ matrix.build.os }}
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - name: Install libpcsclite
        if: matrix.build.platform == 'linux/amd64'
        run: sudo apt install libpcsclite-dev
      - uses: dAppServer/wails-build-action@v2
        with:
          build-name: ${{ matrix.build.name }}
          build-platform: ${{ matrix.build.platform }}
